# .bashrc

# Source global definitions
if [ -f /etc/bashrc ]; then
	. /etc/bashrc
fi

# User specific aliases and functions

alias a1='cd /var/www/ufcwpensionplan.com/'
alias a2='cd /var/www/ufcwpensionplan.com/cibp/'
alias a3='cd /var/log/httpd/'
alias a4='cd /etc/httpd/'
